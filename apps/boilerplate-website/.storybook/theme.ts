import { create } from '@storybook/theming';

export const theme = create({
  base: 'light',
  brandTitle: 'Turborepo Boilerplate - Website',
  brandUrl: 'https://github.com/mkosir/turborepo-boilerplate-multiple-ui',
});
