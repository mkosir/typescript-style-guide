{
  "name": "test-utils",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint --max-warnings 0 .",
    "lint-fix": "eslint --fix .",
    "tsc": "tsc -p tsconfig.json",
    "update-dependencies": "npx npm-check-updates -u"
  },
  "devDependencies": {
    "tsconfig": "*"
  }
}
